# 🚀 Manutech Next.js Deployment Rehberi

## Proje Bilgileri
- **Framework:** Next.js 14.2.13
- **Backend API:** https://demo.eemre.tr/api
- **Tip:** Dinamik içerik (Blog, Kategoriler)

---

## ✅ Seçenek 1: Vercel Deployment (ÖNERİLEN - ÜCRETSİZ)

### Avantajları:
- ✅ Tamamen ücretsiz
- ✅ Otomatik SSL sertifikası
- ✅ Global CDN
- ✅ Otomatik build ve deployment
- ✅ Kendi domain'inizi bağlayabilirsiniz
- ✅ Sınırsız bandwidth

### Adımlar:

1. **GitHub'a projeyi yükleyin:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/KULLANICI_ADINIZ/manutech.git
   git push -u origin main
   ```

2. **Vercel'e kaydolun:**
   - https://vercel.com adresine gidin
   - GitHub hesabınızla giriş yapın

3. **Projeyi import edin:**
   - "New Project" butonuna tıklayın
   - GitHub repository'nizi seçin
   - Framework: Next.js (otomatik algılanır)
   - "Deploy" butonuna tıklayın

4. **Environment Variables ekleyin (gerekirse):**
   - Settings → Environment Variables
   - API_BASE_URL, API_TOKEN gibi değişkenleri ekleyin

5. **Domain bağlayın:**
   - Settings → Domains
   - Kendi domain'inizi ekleyin (örn: manutech.com)
   - DNS ayarlarını yapın

---

## 🔧 Seçenek 2: Paylaşımlı Hosting (Node.js Destekli)

### Gereksinimler:
- ✅ Node.js 18+ desteği
- ✅ SSH erişimi (veya cPanel Node.js App Manager)
- ✅ 4 GB RAM (✅ Sunucunuzda var)
- ✅ 10 GB Disk (✅ Sunucunuzda var)

### Adımlar:

#### 1. Projeyi Build Edin (Local'de):
```bash
cd c:\xampp\htdocs\erkonak.manutech
npm run build
```

#### 2. Dosyaları Hosting'e Yükleyin:

**FTP ile yüklenecek dosyalar:**
- `.next/` (build çıktısı)
- `node_modules/` (tüm bağımlılıklar)
- `public/` (statik dosyalar)
- `package.json`
- `package-lock.json`
- `next.config.mjs`

**Yükleme konumu:** `/home/KULLANICI_ADINIZ/manutech/`

#### 3. cPanel'de Node.js Uygulaması Oluşturun:

1. cPanel → **Setup Node.js App**
2. Yeni uygulama oluştur:
   - **Node.js version:** 18.x veya üzeri
   - **Application mode:** Production
   - **Application root:** `manutech`
   - **Application URL:** `manutech.com` (domain'iniz)
   - **Application startup file:** `node_modules/next/dist/bin/next`
   - **Environment variables:**
     ```
     NODE_ENV=production
     PORT=3000
     ```

3. **Run NPM Install** butonuna tıklayın (eğer node_modules yüklemediyseniz)

4. **Start App** butonuna tıklayın

#### 4. Domain Ayarları:

cPanel → **Domains** → Domain'inizi manutech klasörüne yönlendirin

---

## 🌐 Seçenek 3: Static Export + API (Sınırlı Dinamizm)

**NOT:** Bu yöntem dinamik routing'i desteklemez, sadece build sırasındaki veriler gösterilir.

### next.config.mjs'yi düzenleyin:
```javascript
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
};
```

### Build edin:
```bash
npm run build
```

### `out/` klasörünü hosting'e yükleyin:
- FTP ile `out/` klasörünün içeriğini `public_html/` klasörüne kopyalayın
- Normal HTML sitesi gibi çalışır

**DEZAVANTAJ:** Yeni blog eklendiğinde tekrar build etmeniz gerekir.

---

## 📊 Karşılaştırma Tablosu

| Özellik | Vercel | Node.js Hosting | Static Export |
|---------|--------|-----------------|---------------|
| Maliyet | ÜCRETSİZ | Mevcut hosting | Mevcut hosting |
| Dinamik İçerik | ✅ Tam destek | ✅ Tam destek | ❌ Build-time only |
| Kolay Kurulum | ✅✅✅ | ⚠️ Orta | ✅✅ |
| Performans | ✅✅✅ CDN | ⚠️ Sunucu konumuna bağlı | ✅✅ |
| SSL | ✅ Otomatik | Manuel | ✅ (hosting'den) |
| Güncelleme | ✅ Git push | Manuel FTP | Manuel FTP + Build |

---

## 🎯 TAVSİYE

**Vercel kullanmanızı şiddetle öneriyorum çünkü:**

1. ✅ Tamamen ücretsiz
2. ✅ Otomatik deployment (git push yaptığınızda otomatik güncellenir)
3. ✅ Global CDN ile hızlı erişim
4. ✅ Kendi domain'inizi bağlayabilirsiniz
5. ✅ SSL otomatik
6. ✅ Dinamik içerik tam destekleniyor
7. ✅ Paylaşımlı hosting'inizi Laravel API için kullanabilirsiniz

---

## 🔗 Önerilen Mimari

```
┌─────────────────┐
│  Vercel         │
│  (Next.js App)  │ ← Kullanıcılar buraya erişir
│  manutech.com   │
└────────┬────────┘
         │
         │ API Calls
         ▼
┌─────────────────┐
│  Paylaşımlı     │
│  Hosting        │
│  (Laravel API)  │ ← Backend API burada çalışır
│  demo.eemre.tr  │
└─────────────────┘
```

**Bu yapıda:**
- Frontend (Next.js) → Vercel'de (ücretsiz, hızlı)
- Backend (Laravel API) → Mevcut hosting'inizde
- Her iki sistem birbirinden bağımsız çalışır ve ölçeklenebilir

---

## 📞 Yardım

Hangi yöntemi seçmek istediğinizi söyleyin, adım adım kurulum yapalım!
